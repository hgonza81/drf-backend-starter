services:
  backend:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
      target: ${TARGET}
    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - ../..:/app
    env_file:
      - ../../envs/.env.base
      - ../../envs/.env.${TARGET}
