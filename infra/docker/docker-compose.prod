services:
  backend:
    container_name: backend_prod
    image: docker-backend:prod
    build:
      dockerfile: infra/docker/Dockerfile
      target: prod
    env_file:
      - ../../envs/.env.base
      - ../../envs/.env.prod
